Rename the whole launcher from lantern to glowberry. This includes everything in the app, the repo, the directory and any other references (README/similar). Retheme the app so it fits the new app. Use your design skill, but keep the core concepts.

## Bugs:
- The launch button should be on the most left, and the update and settings should be to the left of it
- The settings and update buttons also should be brighter
- The error box (that shows minecraft error outputs) should have a X to close it on the top right
- The 'offline name' text input box has Apple autocorrect for some reason, which is something we definetely do not want with something like usernames

## New things:
- Fix the 'Set custom path' button in settings, it should open finder or OS alternative to select a folder for it.
- Add/change anything about the whole UI that will fix something, polish something or create a better user expirience! I encourage you to think about this, make it a great app to use for everybody!
